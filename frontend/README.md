# Frontend (Next.js)

1. cd frontend
2. npm install
3. NEXT_PUBLIC_API_URL=http://localhost:4000 npm run dev

Notes:
- frontend uses cookies for auth. All API calls include credentials.
- set NEXT_PUBLIC_API_URL if backend runs on a different host.
